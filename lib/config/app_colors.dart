import 'package:flutter/material.dart';

// Defines the color palette based on NAMA Foundation branding.
class AppColors {
  static const navyBlue = Color(0xFF1B1464); // Primary buttons, navigation bar, headings
  static const goldenYellow = Color(0xFFE4B544); // Accents, highlights, icons, CTAs
  static const darkGray = Color(0xFF4A4A4A); // Body text for readability
  static const lightGray = Color(0xFFF7F6F2); // Section backgrounds, input fields
  static const white = Color(0xFFFFFFFF); // Clean background and negative space

  // Additional colors for UI elements (can be adjusted as needed)
  static const secondaryDarkGray = Color(0xFF6B6B6B);
  static const accentBlue = Color(0xFF3366FF); 
  static const errorRed = Color(0xFFD32F2F); // Standard error color
  
  // Status colors for user management
  static const successGreen = Color(0xFF4CAF50); // For approved status
  static const warningOrange = Color(0xFFFF9800); // For pending status
  static const dangerRed = Color(0xFFE53935); // For rejected status
}